---
"@ccpluginizer/ccpluginizer": patch
---

Verify the new GitHub App release flow end-to-end: this changeset should drive a v0.5.1 bump and the resulting "Version Packages" PR should be authored by `ccpluginizer-release-bot[bot]` with `validate` + `changeset-check` triggering on it automatically.
